generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://gab21:ddtank09@cluster0.v8ntxkx.mongodb.net/appbeta"
}

model Product {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  price       Float
  stock       Int      // Certifique-se de que este campo não permite valores `null`
  category    String   // Adicione este campo
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Order {
  id             String   @id @default(auto()) @map("_id") @db.ObjectId
  productIds     String[] // Lista de IDs dos produtos
  total          Float
  address        String
  paymentMethod  String
  name           String
  email          String
  phone          String
  status         String   // Adicione o campo status
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

// 
